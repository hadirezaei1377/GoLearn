package basics

/*
In Golang, we don't have a built-in function for printing, and we have to use standard libraries...fmt library
*/

fmt.Print("Hi", "")          // Print side by side
fmt.Println("A", "B", "c")   // Print and go to the next line